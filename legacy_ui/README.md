# Legacy UI Quarantine

These files are the pre-React browser UI shell. The production demo path is now the Vite/React app mounted from `index.html` through `src/main.jsx`.

Kept here for reference:

- `js-app.js`
- `js-renderer.js`
- `css-main.css`
- `css-print.css`

Do not edit these for the current MVP unless deliberately restoring the old vanilla interface.
